#arquivo de configuração que executa antes do application.properties
spring.application.name=msbookclient
spring.cloud.config.uri=http://localhost:8888

management.endpoints.web.exposure.include=refresh
